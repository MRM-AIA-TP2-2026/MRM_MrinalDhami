## Adding a new option to continue the movement without needing to launch it again

### Use of spin()
Now te spin() functions takes in the node as an argument, and keeps it alive to listenn for upcoming data ffrom the channel  it is subscribed to, runs the proper callback functions and the keeps the node running as long as we manually shuts is down.

### How do i make the node to keep asking for the values everytime it reaches the destination
So I see two ways to solve it
1:-If there is a way to stop spin in the code itself, we put it through a continuous while which will take in the input, run the node, reaches the destination and shuts down and repeat.

2:- If the node can't be stopped in the code, then we make the spin itslef handle the inputs to and basically make the rest of my code a big while loop

I decide to go with method 1 cause why not eheheh.

Also since i have dealt with the difference in orientation between the imu and gps sensor, I don't need speed inverter anymore. 
