### The current problem is, it is not behaving same for in all sensors. Trying to find the results in different goals in different coordinates.

#### both +ve cooordinates
Moves in both -ve direction
Distance keep increasing.

#### +ve latitude but -ve longitude
Works perfectly fine
Required conditions are met

#### -ve latitude but +ve longitude
Moves in the correct direction, slows down when the reaching the goal but passes over the goal for some reason

#### both -ve coordinates
Same problems as the first condition
going in the opposite direction, and the distance keeps increasing.

### Update-1 Changing the .cpp file
I changed the code to change velocity direction of the rover depending on the type of coordinates.
If the coordinates are of the same type, then the velocity will be negative, that means, the rover will start moving in the opposite direction.

### Update-2 Tinkering with specifications
I have changed the dampeing velocities(both linear and angular) to get more accurate answers. But it seems that, for the 2nd case, due to it trying to get the angular accuracy. It deviates a bit to left causing the error. Will need to fix this.

longitude = +ve yaw_error < 0.03

### A big Update
So after a long time of modifications, I got fed up and what I ended up doing is I change the angle returned by `for_azimuth` to be deviated by -90 degree cause the orientation of gps and imu sensor differ by -90 (I tried changing the reference header but it stayed the same)
